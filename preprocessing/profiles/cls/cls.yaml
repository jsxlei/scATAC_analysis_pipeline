__default__:
  jobname: "{rule}"
  output: ".slurm/{rule}.out"
  error: ".slurm/{rule}.err"
  partition: akundaje,owners,normal
  gres: ""
  time: "8:00:00"
  n_node: 1
  n_task: 1
  n_cpu: "{threads}"
  mem: "20G"

extract_sample_fragments:
  output: ".slurm/{rule}/{wildcards.cell_type}_{wildcards.sample}.out"
  error: ".slurm/{rule}/{wildcards.cell_type}_{wildcards.sample}.err"
  partition: akundaje,owners,normal

callpeak:
  output: ".slurm/{rule}/{wildcards.cell_type}.out"
  error: ".slurm/{rule}/{wildcards.cell_type}.err"
  partition: akundaje,owners,normal

make_bigwig:
  output: ".slurm/{rule}/{wildcards.cell_type}.out"
  error: ".slurm/{rule}/{wildcards.cell_type}.err"
  partition: akundaje,owners,normal

group_celltype:
  output: ".slurm/{rule}/{wildcards.cell_type}_{wildcards.sample}.out"
  error: ".slurm/{rule}/{wildcards.cell_type}_{wildcards.sample}.err"
  partition: akundaje,owners,normal

zcat:
  output: ".slurm/{rule}/{wildcards.sample}.out"
  error: ".slurm/{rule}/{wildcards.sample}.err"
  partition: akundaje,owners,normal